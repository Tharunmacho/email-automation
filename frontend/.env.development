# Dev-only: `next dev` serves the UI on :3000 while FastAPI runs on :8000.
# Production builds (`next build`, output: "export") intentionally leave this
# unset so every API call stays same-origin under the FastAPI server.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
