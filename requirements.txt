# ---- Core ----
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ---- Gmail API + OAuth2 ----
google-api-python-client>=2.130
google-auth>=2.30
google-auth-oauthlib>=1.2

# ---- AI (Anthropic Claude) ----
anthropic>=0.40

# ---- Database ----
pymongo>=4.8

# ---- File type detection ----
filetype>=1.2            # pure-python, no libmagic needed (Windows-friendly)

# ---- Text extraction ----
PyMuPDF>=1.24            # fitz: PDF text + render pages to images for OCR
python-docx>=1.1         # .docx
docx2txt>=0.8            # fallback .docx text

# ---- OCR (scanned PDFs / images) ----
pytesseract>=0.3.10      # requires Tesseract-OCR binary installed on the host
Pillow>=10.3

# ---- REST API (extension seam) ----
fastapi>=0.111
uvicorn[standard]>=0.30

# ---- Background jobs (extension seam) ----
celery>=5.4
redis>=5.0

# ---- CLI ----
typer>=0.12

# ---- Dev / tests ----
pytest>=8.2
